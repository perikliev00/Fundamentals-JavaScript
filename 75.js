function matrix(numb) {
    for(let i=0;i<numb;i++) {
        let arr=[];
        for(let x=0;x<numb;x++) {
            arr.push(numb);
        }
        console.log(arr.join(` `))
    }
}
matrix(2);
